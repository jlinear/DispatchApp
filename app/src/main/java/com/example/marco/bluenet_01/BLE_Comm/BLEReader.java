package com.example.marco.bluenet_01.BLE_Comm;

import nd.edu.bluenet_stack.AdvertisementPayload;
import nd.edu.bluenet_stack.LayerBase;
import nd.edu.bluenet_stack.Query;

public class BLEReader extends LayerBase implements Query {

    //Hashmap of connections, BT addr, and BN addr

    public BLEReader () {
        //set up scanner
        //connect to GATT services
    }


    public String ask(String question) {
        String resultString = new String();

        return resultString;
    }
}
